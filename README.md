### This project will enpoints to run a file and store in AWS S3.
### Later on same project I will use for reading file from S3 and store data into database, this will be a Spring batch.

### Make a lambda method to read file from s3 and print.